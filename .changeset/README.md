# Changesets

Use this folder to describe pending package releases.

Create a changeset after changing package code:

```bash
npm run changeset
```

Apply version bumps and changelog updates:

```bash
npm run version-packages
```

Publish changed packages:

```bash
npm run release
```
